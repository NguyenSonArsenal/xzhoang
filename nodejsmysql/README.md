How to run:
OS: ubuntu
install : `sudo apt update && sudo apt install npm && sudo apt install nodejs`

Run project with:  

Update host, user, pass: server.js  

`npm install`
`node server.js`

docker compose up -d --build   
On browser: 127.0.0.1:3000
